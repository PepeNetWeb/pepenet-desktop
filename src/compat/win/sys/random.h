/* sys/random.h — Windows stand-in: getentropy over BCryptGenRandom (a real
 * CSPRNG — this seeds private keys). Implemented in win_compat.c. */
#ifndef DNET_COMPAT_SYS_RANDOM_H
#define DNET_COMPAT_SYS_RANDOM_H
#include "../dnet_wincompat.h"
#endif
